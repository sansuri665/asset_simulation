name: Directional Oil Calibration

on:
  push:
    branches:
      - main
      - 'calibration/directional-*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Full Python unit suite
        run: python -m unittest discover -s asset_simulation/tests -v

  economic-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Directional economic smoke
        run: |
          python -m asset_simulation.audit_oil_directional_economic_calibration \
            --seeds 0 \
            --years 1 \
            --summary-only \
            --output artifacts/directional-economic-smoke.json
      - name: Upload smoke report
        uses: actions/upload-artifact@v4
        with:
          name: directional-economic-smoke
          path: artifacts/directional-economic-smoke.json

  full-calibration:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Formal account 5-year calibration
        run: |
          mkdir -p artifacts
          python -m asset_simulation.audit_oil_formal_account_calibration \
            --seeds 42,2026,7777,9001,314159,271828 \
            --years 5 \
            --output artifacts/formal-account-5y.json
      - name: Controlled directional calibration
        run: |
          python -m asset_simulation.audit_oil_directional_economic_calibration \
            --seeds 0,1,2,3,4,5,6,7 \
            --years 3 \
            --summary-only \
            --output artifacts/directional-economic-calibration.json
      - name: Regime audit
        run: python -m asset_simulation.audit_oil_direction_regimes > artifacts/directional-regimes.json
      - name: Enforce formal-account realism gate
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          report = json.loads(Path('artifacts/formal-account-5y.json').read_text())
          print('hard_gate_pass=', report.get('hard_gate_pass'))
          print('realism_gate_pass=', report.get('realism_gate_pass'))
          print('cagr_median=', report['distributions']['cagr_pct']['median'])
          if not report.get('hard_gate_pass'):
              raise SystemExit('formal-account hard gate failed')
          if not report.get('realism_gate_pass'):
              raise SystemExit('formal-account realism gate failed')
          PY
      - name: Enforce directional economic gates
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          report = json.loads(Path('artifacts/directional-economic-calibration.json').read_text())
          failed = [name for name, passed in report.get('gates', {}).items() if not passed]
          print('failed_gates=', failed)
          if failed:
              raise SystemExit('directional economic gates failed: ' + ', '.join(failed))
          PY
      - name: Upload calibration reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: directional-oil-calibration-reports
          path: artifacts/*.json
