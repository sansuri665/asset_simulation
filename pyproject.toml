[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "asset-simulation"
version = "0.2.0"
description = "Deterministic global macro, physical oil, and crude-shipping demand simulation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
asset-simulation = "asset_simulation.server:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["asset_simulation*"]

[tool.setuptools.package-data]
asset_simulation = [
  "config/*.json",
  "contracts/*.json",
  "docs/*.md",
  "docs/**/*.md",
  "viewer/*.html",
  "viewer/css/*.css",
  "viewer/js/*.js",
]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]
